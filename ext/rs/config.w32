// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("rs", "for rs support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("rs", "enable rs support", "no");

if (PHP_RS != "no") {
	EXTENSION("rs", "rs.c");
}

